# [Info - Build Information](https://github.com/embtool/info) as an External Project

Set-up and build:

```console
$ cmake -B build/
$ cmake --build build/
$ ./build/main
```

Re-build:

```console
$ cmake --build build/
$ ./build/main
```
